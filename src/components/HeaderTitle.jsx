

export const HeaderTitle = ({titleText}) => {

    useEffect(() => {
    }, [])

    return (
        <div className="titleText">
            <h1>{titleText}</h1>
            
        </div>
    )
}